/* index.js — DEAD CODE.
   index.html is rendered as the social feed by geohub-social-redesign.js → renderFeed().
   The stat-counter animation (.landing-stat[data-count]) targeted a previous landing page
   that no longer exists. Kept as an empty stub to avoid 404s. */
